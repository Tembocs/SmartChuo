# SmartChuo

- This a monorepo that has several projects in it.
- For all Flutter project use `flutter_bloc`, `equatable` packages for state management.
- Use `get_it` package for dependecy injection.
- Use `sqlite3` package for local database.
- Document all the code by putting the comments/docs above the responsible code.
- Do not litter the root of the monorepo.